package com.example.popularmovietask.adapters

import com.example.popularmovietask.R

/*
 *  Enum class for different item types(layouts) for adapter
 **/
enum class ItemType(val id: Int) {
    POPULAR_MOVIES(R.layout.popular_movie_list_layout)
}